SELECT COALESCE(CAST(MIN(column) AS VARCHAR(50)),'-')
FROM db.table
WHERE (TYPE(column) LIKE '%INT%'
OR TYPE(column) LIKE '%TIME%'
OR TYPE(column) LIKE '%INTERVAL%'
OR TYPE(column) IN ('DECIMAL','NUMERIC','FLOAT','REAL','DOUBLE','PRECISION','PRECISION','DATE'))